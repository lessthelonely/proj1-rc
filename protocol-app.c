#include <sys/types.h>
#include <sys/stat.h>
#include <fcntl.h>
#include <termios.h>
#include <stdio.h>
#include <signal.h>
#include "constants.h"

int llopen(int porta,int sender){ //sender will be TRANSMITTER ou RECEIVER

}

int llwriter(int fd, char*buffer,int length){

}

int llread(int fd,char*buffer){

}

int llclose(int fd){
    
}